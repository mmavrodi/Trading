# Trading
The application should generate market prices, process and store them, validate trade requests, apply configurable trading rules, generate automatic orders, persist data in a database, and expose HTTP API endpoints.
